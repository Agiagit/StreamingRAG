# Streaming RAG - shared dependencies
# Python: 3.12 (most mature version with fully supported torch/transformers wheels;
# create this project's venv on 3.12 even if another env on your machine is newer)

# --- Web backend ---
fastapi>=0.110
uvicorn[standard]>=0.30

# --- Embeddings and retrieval ---
# sentence-transformers provides all-MiniLM-L6-v2.
sentence-transformers>=3.0
numpy>=1.26

# --- Local answer generation ---
transformers>=4.44
accelerate>=0.33

# --- PyTorch ---
# Pinned to a fixed VERSION but not a build, so the whole team gets the same torch
# that transformers was tested against (this is what prevents the import break we hit).
# A plain install of this line gives the portable CPU build, which runs on any machine
# with no GPU required.
# Optional GPU acceleration: reinstall this SAME version from the matching CUDA index
# (see the README). Keeping the version identical preserves transformers compatibility;
# only the CPU-vs-CUDA build changes.
torch==2.12.0

# If you run generation through Ollama instead (separate install, not pip), you do not
# need transformers/accelerate/torch for generation; you would call the local Ollama
# endpoint from the backend.
